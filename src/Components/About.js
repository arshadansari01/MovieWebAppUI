import React, { useState } from 'react'

export default function About(props) {
   


return (
    <div>

        <button onClick={props.buttonclick} type="button" > About us</button>

    </div>
)
}
